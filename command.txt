git init => for initailize git

git status => list all untrackedfiles

git add -A => add all untracked files to staging area

git commit -m "added" => staging area => local repository 

git config --global user.name "username"

git config --global user.email "email"

git branch

git checkout